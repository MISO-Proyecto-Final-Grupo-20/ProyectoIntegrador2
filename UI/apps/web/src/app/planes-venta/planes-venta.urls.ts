import { environment } from '../../environments/environment';

export const PlanesVentaUrls = {
  obtenerPeriodosTiempo: `${environment.apiUrl}/periodosTiempo`,
  planesVenta: `${environment.apiUrl}/planesVenta`,
};
