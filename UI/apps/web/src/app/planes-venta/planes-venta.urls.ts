import { environment } from '../../environments/environment';

export const PlanesVentaUrls = {
  obtenerPeriodosTiempo: `${environment.apiUrl}/ventas/periodosTiempo`,
  planesVenta: `${environment.apiUrl}/ventas/planesVenta`,
};
